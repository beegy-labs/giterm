# Q1 2026 v0.2 Tasks — Mobile SSH Enhancements

> Detailed task breakdown | **Last Updated**: 2026-03-22

## Phase 1: iOS UX Bug Fixes

- [x] Korean IME backspace: timer callback executes all actions including swap
- [x] Korean IME: `executeActions(actions, false)` in DELETE_TIMER (50ms async safe)
- [x] Password persistence: `partialize` passes full state (no longer strips secrets)
- [x] Password persistence: tauriStorage JSON as primary, keychain as fallback

## Phase 2: iOS Keyboard Resize

- [x] `useVisualViewport`: debounce resize/scroll handlers to 100ms
- [x] `useTerminalInstances`: debounce `fitAddon.fit()` to 100ms
- [x] `useTerminalInstances`: debounce `sshResize()` IPC to 150ms after fit
- [x] Research: `env(keyboard-inset-*)`, `navigator.virtualKeyboard`, `interpolate-size` NOT supported in Safari/WKWebView (2026)

## Phase 3: Resource Management

- [x] `SshSessionManager::disconnect_all()` — iterates + disconnects all sessions
- [x] `TunnelManager::stop_all()` — iterates + stops all tunnels
- [x] `DebugLogManager::cleanup()` — deletes `/tmp/giterm-*.log` on exit
- [x] `lib.rs`: `RunEvent::Exit` handler calls cleanup in order
- [x] `#[derive(Clone)]` on SshSessionManager + TunnelManager for handler access
- [x] `pub(crate)` on ImeLogState/VpLogState inner fields

## Phase 4: Layout & Safe Area

- [x] `MobileLayout`: add `pt-safe-bar` as safe area SSOT
- [x] `MobileSessionTabBar`: remove duplicate `pt-safe-bar`
- [x] `MobileScreen.Header`: change `pt-safe-header` → `pt-1` (no duplicate safe area)
- [x] `MobileScreen.Bar`: remove `pt-safe-bar`
- [x] `DevFrame`: offset labels below safe area for `pt-safe` elements
- [x] `MobileScreen.Header`: reduce padding (`pb-3` → `pb-2`)
- [x] `index.css`: reduce `pt-safe-header` extra padding (`0.75rem` → `0.25rem`)

## Phase 5: Monetization Groundwork

- [x] `features/ad-banner/`: create feature with FSD structure
- [x] `AdBanner` component: 50px fixed-height slot, hidden when no ad
- [x] `useAdBannerStore`: Zustand store with `isAdLoaded` state
- [x] `MobileLayout`: integrate AdBanner above SessionTabBar
- [ ] AdMob SDK: Tauri iOS Swift plugin (pending)
- [ ] Ad unit ID configuration (pending)
- [ ] GDPR/ATT consent flow (pending)

## Phase 6: CDD Updates (2026-03-12)

- [x] `docs/llm/apps/giterm.md`: update credential storage, resize debounce, safe area, exit cleanup
- [x] `.ai/apps/giterm.md`: add ad-banner feature, update features table
- [x] `docs/llm/features/ssh-connect.md`: update credential storage section
- [x] `docs/llm/features/korean-ime.md`: add timer callback fix note
- [x] `docs/llm/features/ios-caret-fix-impl.md`: update Layer 5 safe area, Layer 8 debounce
- [x] `docs/llm/features/ad-banner.md`: new feature SSOT
- [x] `docs/llm/features/app-lifecycle.md`: new feature SSOT
- [x] `.specs/apps/giterm/roadmap.md`: add M2.5 milestone
- [x] `.specs/apps/giterm/scopes/2026-Q1-v02.md`: new scope
- [x] `.specs/apps/giterm/tasks/2026-Q1-v02.md`: new task breakdown

## Phase 7: iOS Viewport + Tab + Input Fixes (2026-03-22)

- [x] `src-tauri/src/lib.rs`: iOS safe area native ObjC fix (retry loop [0,16,50,100,250,500,1000]ms)
  - `read_safe_area_insets()`: wk.safeAreaInsets → window.safeAreaInsets fallback
  - `neutralize_controller_safe_area()`: additionalSafeAreaInsets = negative compensation
  - `apply_viewport_insets()`: setMinimumViewportInset = setMaximumViewportInset
  - `pin_webview_to_window_bounds()`: WKWebView + superview + scrollView frame = window.bounds
  - `apply_safe_area_to_dom()`: inject --sat, --sab, cache in localStorage, dispatch event
  - WKWebView scrollView: contentInsetAdjustmentBehavior=.never, scrollEnabled=false, bounces=false
- [x] `src/shared/lib/useVisualViewport.ts`: remove env() fallback, use --sat/--sab only; listen for `giterm:safe-area-ready` event
- [x] `src/index.css`: `.pt-safe-bar`/`.pt-safe-header` use `var(--sat)` only (no env()); `#root` opacity hidden until `data-ios-safe-area-ready`
- [x] `src/shared/ui/mobile-screen.tsx`: Header uses `pt-safe-header`, Bar uses `pt-safe-bar`
- [x] `src/widgets/mobile-layout/ui/MobileSessionTabBar.tsx`: add `pt-safe-bar`; fix tab key to `connectionId-index` (prevents DOM recreation on reconnect)
- [x] `src/widgets/mobile-layout/ui/MobileLayout.tsx`: remove `pt-safe-bar` from container; terminal section uses `flex-1` (not `--vvh`)
- [x] `src/widgets/terminal-view/ui/TerminalView.tsx`: overlay pattern for all status overlays (absolute inset-0) — prevents early returns that destroyed xterm instances
- [x] `src/shared/lib/iosInputFix.ts`: new SSOT for iOS input focus scroll-into-view
- [x] `src/shared/ui/input.tsx`: add iosInputFocusFix onFocus handler
- [x] `src/features/ssh-connect/adapters/api/sshApi.ts`: add `clearResizeCache(sessionId)` export
- [x] `src/features/ssh-connect/model/closeSession.ts`: call `clearResizeCache` before removeSession

## Phase 8: CDD Updates (2026-03-22)

- [x] `docs/llm/features/ios-viewport.md`: new SSOT for iOS viewport safe area fix
- [x] `docs/llm/apps/giterm.md`: update Known Pitfalls (viewport shrink, safe area CSS, input zoom, overlay pattern, WebGL limit)
- [x] `.ai/apps/giterm.md`: add iosInputFix.ts, ios-viewport.md, new features, Pro Max dev command
- [x] `.specs/apps/giterm/roadmap.md`: M2.5 marked Completed, delivered items updated
- [x] `.specs/apps/giterm/scopes/2026-Q1-v02.md`: add Phase 7 features, mark Completed
- [x] `.specs/apps/giterm/tasks/2026-Q1-v02.md`: add Phase 7+8 task breakdown
